第十一章首先介绍了运算符重载的方法。通过运算符重载可以更加灵活地实现对象之间的运算关系。再介绍了友元，可以通过友元来访问类中的私有成员。但是使用友元会破坏封装性，影响程序的安全性。接下来介绍了类的强制转换与自动转换。通过在类的声明过程中定义转换函数实现类的自动转换。

第十二章介绍了类和动态内存分配。可以通过一个指针类型的str，先不定义其容量大小，而是根据数据长度使用new来分配内存给str。并且如果在构造函数中使用new来初始化指针成员，则应在析构函数中使用delete。如果静态成员是const整数类型或枚举类型,则可以在类声明中初始化。