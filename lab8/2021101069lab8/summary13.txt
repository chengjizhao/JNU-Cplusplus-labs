第13章类继承
基类和派生类：基类是创建派生类的模板，派生类从基类继承属性和方法。派生类对象也是一个基类对象，继承了基类的所有公有和保护成员。
派生类的构造函数：在创建派生类对象时，程序首先生成基类对象。除非使用默认构造函数，否则应通过成员初始化列表显式调用正确的基类构造函数。
虚函数和重写：同一方法随上下文而异，应当使用虚方法。在派生类中使用作用域解析符可将基类虚方法同派生类版本的虚方法区分开来。派生类应重新定义基类的所有重载版本。
访问控制：派生类对象可初始化或赋值基类对象，前者的基类部分复制给后者。但派生类不能直接访问基类的私有成员，只能通过基类的公有和保护方法访问。
