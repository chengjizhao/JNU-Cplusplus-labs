external变量是全局变量，可以在头文件中申明某个变量是全局变量，如external int a;后面赋值就成为定义，到其他地方在定义就是重复定义。
申明后， 把这个头文件包含在其他的.cpp中间，可以在任何一个cpp中间定义，但是不能定义两次。使用这个变量其实地址都是一个地址，不会改变。
Static 关键字
静态局部变量
在局部变量前，加上关键字static，该变量就被定义成为一个静态局部变量。
namespace直译就是命名空间的意思，它是一种逻辑分组机制，将按照某些标准在逻辑上属于同一个任务的所有类声明放在同一个命名空间。