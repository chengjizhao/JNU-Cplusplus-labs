第5章 - 循环和关系表达式
1.  循环结构：
 `for` 循环
 `while` 循环
 `do-while` 循环
2.  `break` 和 `continue`：
 `break` 用于跳出循环
 `continue` 用于结束当前循环迭代并进入下一次迭代
3.  关系表达式：
 关系运算符如 `==`, `!=`, `<`, `>`, `<=`, `>=`

 第6章 - 分支语句和逻辑运算符

1.  分支结构：
`if` 条件语句
`if-else` 分支结构
`switch` 多重选择语句
2.  逻辑运算符：
`&&` (逻辑与)
`||` (逻辑或)
`!` (逻辑非)
3. 条件运算符 (`? :`)
使用 `? :` 来进行条件表达式

第7章 - 函数--C++的编程模块

1.  函数定义和调用：
函数声明和定义
函数参数和返回值
函数重载
2.函数原型：
 函数原型的作用和语法
3.  函数参数传递：
 值传递
 引用传递
4.  函数重载：
同名函数但参数列表不同的情况
5.  递归函数：
函数调用自身的概念和应用

第8章 - 存储类和自动存储持续性

1.  存储类：
`auto`, `register`, `static`, `extern` 存储类的概念和用法
2.  作用域和生存期：
作用域规则
生命周期和存储类关联
3.  常量：
`const` 关键字
常量引用

第9章 - 对象和类

1.  类和对象：
类的定义和声明
对象的创建和使用
2.  类的成员：
数据成员
成员函数
3.  访问控制：
`public`, `private`, `protected` 访问修饰符
4.  构造函数和析构函数：
构造函数的定义和作用
析构函数的定义和作用
5. this 指针：
this 指针的概念和使用
