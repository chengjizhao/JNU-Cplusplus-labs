序列容器：vector、list、deque、string。
关联容器：set、multiset、map、multimap。
vector：
  优点：
    1.在末尾插入数据时速度快（时间固定）
    2.可以像访问数组一样访问
  缺点：
    1.调整大小时将影响性能
    2.搜索时间与容器包含的元素个数成反比
    3.只能在末尾插入数据(不用insert)
deque：
  优点：
    1.有vector的所有优点
    2.还可以在容器开头插入或删除数据
  缺点：
    1.有vector的所有缺点
list：
优点：
    1.可在开头、中间、结尾插入或删除数据
    2.插入或删除元素后、指向list中其他元素的迭代器仍有效
  缺点：
    1.不能像数组那样访问元素
    2.搜索速度比vector慢
set：
  优点：
    搜索速度比顺序容器快
  缺点：
    元素的插入速度比顺序容器慢
map：
  优点：
    用于存储键-值对的容器，搜索时间快
  缺点：
    元素未被严格排序
