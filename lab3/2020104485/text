STL（Standard Template Library）标准容器是C++标准库中提供的一组数据结构，具有以下特点：

1.通用性：STL标准容器是通用的，可以存储不同类型的数据，包括基本类型和自定义类型。

2. 动态大小：STL标准容器的大小可以根据需要进行动态调整，可以根据实际情况自动扩展或收缩。

3. 高效性：STL标准容器在设计上追求高效性，提供了高效的插入、删除和查找操作，以及迭代器等功能。

4. 内存管理：STL标准容器自动管理内存，无需手动分配和释放内存，减少了内存管理的复杂性。

5. 提供多种容器类型：STL标准库提供了多种容器类型，包括向量（vector）、链表（list）、队列（queue）、栈（stack）、映射（map）等，可以根据不同的需求选择合适的容器类型。

典型的使用场景：
1. 向量（vector）：适用于需要随机访问元素、动态调整大小的场景，例如存储动态数组、实现动态数组的栈等。

2. 链表（list）：适用于频繁的插入和删除操作，不需要随机访问元素的场景，例如实现队列、实现LRU缓存等。

3. 队列（queue）：适用于先进先出（FIFO）的数据结构，例如任务调度、消息队列等。

4. 栈（stack）：适用于后进先出（LIFO）的数据结构，例如函数调用栈、表达式求值等。

5. 映射（map）：适用于键值对存储
